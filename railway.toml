[build]
builder = "NIXPACKS"
buildCommand = "pnpm turbo run build --filter=@swarm/orchestrator..."

[deploy]
startCommand = "pnpm --filter @swarm/orchestrator start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
